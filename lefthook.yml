pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx}'
      run: tsc -p ./packages/client/tsconfig.json --noEmit && tsc -p ./packages/server/tsconfig.json --noEmit && yarn eslint {staged_files}
